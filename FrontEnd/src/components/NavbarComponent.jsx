// Navbar Component
import React from "react";

const NavbarComponent = ( logo, links, buttons ) => {
    return (
        <div>
            <div>
                <div>
                    <img src="{ logo }" alt="" />
                </div>
            </div>
        </div>
    )
};